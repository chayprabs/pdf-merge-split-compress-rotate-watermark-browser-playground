export function SeoBar() {
  return (
    <section
      className="border-b border-neutral-100 bg-neutral-50"
      aria-label="About Press"
    >
      <div className="mx-auto max-w-5xl px-4 py-3 text-center text-sm leading-relaxed text-neutral-600">
        <p>
          Merge, split, compress, rotate, and watermark PDF files entirely in
          your browser — no uploads, no account, no waiting.
        </p>
        <p className="mt-0.5">
          Powered by WebAssembly (pdfcpu). Your documents never leave your
          device.
        </p>
      </div>
    </section>
  );
}
